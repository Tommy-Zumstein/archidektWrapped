import archidekt from './config'

const deckURI = (id, small=false) => {
  let uri = `decks/${id}/`
  if (small) uri += 'small/'
  return uri
}

export const fetchDeckById = async (id, small=false) => {
  let response = await archidekt.get(deckURI(id, small))
  return response
}
